[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "artificial-ecology"
version = "0.1.0"
description = "A deterministic foundation for an open-ended artificial ecology"
requires-python = ">=3.11"
dependencies = []
